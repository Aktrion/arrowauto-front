version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm use 20
        - npm ci
    build:
      commands:
        - npm run build
    postBuild:
      commands:
        - echo "Build completed on `date`"
  artifacts:
    baseDirectory: dist/arrowauto/browser
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
